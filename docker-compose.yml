services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    volumes:
      - ./backend/models_store:/app/models_store
      - ./dataset:/dataset
    environment:
      - DATABASE_URL=postgresql://sih:sih@db:5432/sih
      - MAXMIND_ACCOUNT_ID=${MAXMIND_ACCOUNT_ID}
      - MAXMIND_LICENSE_KEY=${MAXMIND_LICENSE_KEY}
      - ABUSEIPDB_API_KEY=${ABUSEIPDB_API_KEY}
      - IPINFO_TOKEN=${IPINFO_TOKEN}
    depends_on: [db]

  frontend:
    build: ./frontend
    # The production image uses nginx, which listens on port 80 inside
    # the container. Keep 5173 as the browser-facing port for the team.
    ports: ["5173:80"]
    depends_on: [backend]

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: sih
      POSTGRES_PASSWORD: sih
      POSTGRES_DB: sih
    volumes: ["pgdata:/var/lib/postgresql/data"]

volumes:
  pgdata:
